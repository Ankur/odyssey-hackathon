VITE_ODYSSEY_API_KEY=ody_your_api_key_here
# Auth for Vercel AI Gateway (server-side only, not exposed to browser).
# Option 1: Use 'vercel dev' for automatic OIDC auth (no key needed).
# Option 2: Set AI_GATEWAY_API_KEY here for plain 'npm run dev'.
AI_GATEWAY_API_KEY=your_vercel_ai_gateway_key_here
